# 정보처리기사 필기 스터디 로그

> 목표 시험일: **2026-08-07** · D-28
> 진행: **Day 60/88** (68%)

## 진행도
`███████████████▒▒▒▒▒▒▒` 60/88

## 88일 커리큘럼

### Phase 1: 1회독 (Day 1-25)

#### 2과목 소프트웨어 개발 (Day 1-5)
- [x] Day 1 - 자료구조 (스택/큐/덱/리스트/트리/그래프)
- [x] Day 2 - 알고리즘 복잡도(빅O) + 정렬 알고리즘
- [x] Day 3 - 탐색 + 해시 + 그래프 알고리즘
- [x] Day 4 - 소프트웨어 테스트 (블랙/화이트박스, V모델)
- [x] Day 5 - 통합 구현 + 형상관리(Git/SVN) + 인터페이스 + 패키징

#### 4과목 프로그래밍 언어 활용 (Day 6-10)
- [x] Day 6 - C 언어 (포인터, 구조체, 배열, 함수, 동적할당)
- [x] Day 7 - Java 객체지향 (상속, 다형성, 추상, 캡슐, 예외, 컬렉션)
- [x] Day 8 - Python (자료형, 컬렉션, 제어문, 클래스)
- [x] Day 9 - 운영체제 (프로세스/스레드, 스케줄링, 메모리 관리, 페이지 교체)
- [x] Day 10 - 네트워크 (OSI 7계층, TCP/IP, IPv4/v6, 라우팅)

#### 1과목 소프트웨어 설계 (Day 11-15) ★ 전용 포맷
- [x] Day 11 - 생명주기·개발 방법론 (폭포수/나선형/애자일/스크럼/XP/Lean/Kanban)
- [x] Day 12 - 요구사항 분석 (기능/비기능, 요구공학)
- [x] Day 13 - UML 다이어그램 13종 + SOLID 원칙
- [x] Day 14 - 디자인 패턴 GoF 23 (생성5/구조7/행위11)
- [x] Day 15 - 아키텍처 패턴 + UI/UX + 인터페이스 설계(EAI/ESB)

#### 3과목 데이터베이스 구축 (Day 16-20)
- [x] Day 16 - DB 개요, 스키마 3단계, ERD, 관계대수·해석
- [x] Day 17 - 정규화 (1NF~BCNF, 4NF, 5NF), 함수종속성
- [x] Day 18 - SQL DDL/DML/DCL + 조인 + 서브쿼리 + 뷰 + 인덱스
- [x] Day 19 - 트랜잭션 ACID + 격리수준 + 락 + 회복 + 동시성 제어
- [x] Day 20 - DB 설계 + 분산 DB(4투명성) + NoSQL

#### 5과목 정보시스템 구축관리 (Day 21-25)
- [x] Day 21 - 개발 방법론(CMMI/SPICE/V&V) + 비용산정(LOC/COCOMO/FP/델파이/Putnam) + 일정(PERT/CPM/Gantt)
- [x] Day 22 - IT 신기술 키워드 60+ (블록체인/AI/IoT/클라우드/메타버스)
- [x] Day 23 - 정보보안 기본 (대칭/비대칭/해시, 접근통제, 인증)
- [x] Day 24 - 네트워크 보안 (방화벽/IDS/IPS/VPN/DDoS/APT)
- [x] Day 25 - 시스템·앱 보안 (버퍼오버플로우/접근통제/악성코드/Secure SDLC/SQLi·XSS·CSRF)

### Phase 2: 2회독 + 기출 집중 (Day 26-45)
- [x] Day 26 - 자료구조 심화 (스택 응용/큐/덱/트리 순회/BST·힙/그래프 표현)
- [x] Day 27 - 알고리즘·정렬 심화 (빅O/버블·선택·삽입 추적/퀵·합병·힙/이분탐색/설계기법)
- [x] Day 28 - 탐색·해시·그래프 심화 (순차/이진/보간/해시 충돌·그래프 표현·MST/최단경로)
- [x] Day 29 - 소프트웨어 테스트 + 통합·형상·인터페이스 (블랙/화이트박스·V모델·통합방식·SCM·EAI/ESB·JSON)
- [x] Day 30 - C 언어 심화 (2차원 배열·단축평가·문자열·재귀·비트시프트·static·구조체배열·문자열포인터)
- [x] Day 31 - Java 객체지향 심화 (상속·오버라이딩·생성자호출순서·다형성·추상/인터페이스·예외흐름·접근제어자·컬렉션)
- [x] Day 32 - Python 심화 (컬렉션4종·슬라이싱·컴프리헨션·딕셔너리메소드·문자열·클래스/상속·예외·람다)
- [x] Day 33 - OS + 네트워크 통합 심화 (스케줄링5종·페이지교체4종·교착상태·OSI7계층·TCP/UDP·IPv6·서브넷·라우팅)
- [x] Day 34 - 1과목 생명주기·개발방법론 + 요구사항분석 통합 (폭포수/나선형/스크럼/XP/요구공학5단계/DFD/자료사전)
- [x] Day 35 - UML 다이어그램 14종(구조7/행위7) + SOLID 5원칙(단·개·리·인·의) + 럼바우 3모델링 + 유스케이스 4관계
- [x] Day 36 - 디자인 패턴 GoF 23 통합 심화 (생성5/구조7/행위11 + 정형 키워드 매칭)
- [x] Day 37 - 소프트웨어 아키텍처 + 모듈 응집도·결합도 통합 (아키텍처7패턴/MVC/파이프필터/Kruchten4+1/ATAM)
- [x] Day 38 - 3과목 DB 개론 + Key 7종 + 무결성 제약 (DBMS3기능/3단계스키마/후보·기본·대체·슈퍼·외래·유일·복합키/개체·참조·도메인·사용자정의 무결성/Chen ERD)
- [x] Day 39 - 3과목 정규화(1NF~BCNF) + 함수 종속 + 이상현상 (도부이결다조/완전·부분·이행 FD/삽삭갱 Anomaly/BCNF 결정자=후보키)
- [x] Day 40 - 3과목 SQL DDL/DML/DCL + 조인 + 서브쿼리 + 뷰 + 인덱스 (정조제/DROP·TRUNCATE·DELETE 비교/WHERE↔HAVING/JOIN5종 m×n/단일·다중행 연산자/뷰 ALTER 불가/인덱스 trade-off)
- [x] Day 41 - 3과목 트랜잭션 ACID + 격리수준 + 락 + 회복 + 분산 DB + NoSQL (원일격영/상태5종/RU<RC<RR<S/Dirty·Non-Repeatable·Phantom 매트릭스/S·X-Lock+2PL/UNDO·REDO·검사점/4투명성/NoSQL 4유형 + CAP)
- [x] Day 42 - 5과목 개발 방법론(CMMI/SPICE/V&V) + 비용 산정(LOC/COCOMO/FP/델파이/Putnam) + 일정(PERT/CPM/Gantt) (하·상·수 분류/LOC (a+4m+b)/6 공식/COCOMO 3유형 5만·30만 임계/Putnam-SLIM-Rayleigh/FP-Albrecht-ESTIMACS/CMMI 초·관·정·정·최/SPICE 불·수·관·확·예·최 0단계부터/V&V right product vs product right/임계경로=가장 긴 경로·Slack 0)
- [x] Day 43 - 5과목 IT 신기술 키워드 통합 (클라우드 IaaS/PaaS/SaaS/BaaS=Blockchain/블록체인·메타버스·디지털트윈·매시업/빅데이터 Hadoop·HDFS·MapReduce·Spark·Tajo=국산SQL·Sqoop=RDB↔Hadoop/무선통신 Zigbee·Beacon·Wi-SUN·NB-IoT·LTE-M/포그=중간계층·엣지=단말/큐비트·Memristor 4번째수동소자·DRM=저작권/OTT·N-Screen·IPTV·MVNO=알뜰폰·MNO=1차)
- [x] Day 44 - 5과목 정보보안 + 네트워크보안 통합 (대칭/비대칭키 분류·국산SEED·ARIA·HIGHT·LEA/해시 MD5=128·SHA-1=160·SHA-256·SHA-512·HAS-160=국산160/접근통제 MAC=강제·DAC=임의·RBAC=역할·ABAC=속성/AAA 인증·인가·과금+RADIUS·DIAMETER·TACACS+/인증3요소 지식·소유·존재+Kerberos=티켓·MIT/DoS SYN Flood·Smurf=ICMP브로드캐스트·Land=출발지=목적지·Teardrop=단편offset/사회공학 피싱·스미싱=SMS·파밍=DNS hosts/웹공격 SQLi·XSS·CSRF/방화벽L3·4·IDS수동·IPS능동·WAF L7·IPSec=L3·SSL=L4~7/DSA=서명전용·DH=키교환전용·MAC약어충돌·PKI=공개키X.509CA)
- [x] **Day 45 - 5과목 앱·시스템 보안 통합 (BoF 4종 Stack·Heap·FormatString·Integer/대응 ASLR·DEP=NX·StackCanary/악성코드 11종 Virus숙주·Worm네트워크·Trojan위장·Rootkit은폐·Backdoor통로·Ransomware암호화·Spyware수집·Keylogger키입력·Adware광고·LogicBomb조건·APT표적/Secure SDLC 4모델 MS-SDL·MS / SevenTouchpoints·McGraw / CLASP·OWASP / SAMM·OWASP/KISA 시큐어코딩 7대 카테고리 입력검증·보안기능·시간상태·에러·코드오류·캡슐화·API오용/OWASP Top10 2021 A01~A10 SSRF신규/권한상승 수직·수평·SetUID·Rootkit시퀀스) — Phase 2 2회독 최종일** ← 오늘

### Phase 3: 5개년 기출 회독 (Day 46-65)
- [x] Day 46-49: 2021년 ← **2021년 회독 완료 (4/4)**
- [x] Day 50-53: 2022년 ← **2022년 회독 완료 (4/4)**
- [x] Day 54-57: 2023년 ← **2023년 회독 완료 (4/4)**
- [x] Day 58-60: 2024년 (진행중, 회독 3/4)
- [ ] Day 61: 2024년 (회독 4/4 · 최종회독)
- [ ] Day 62-65: 2025년

### Phase 4: 마무리 (Day 66-88)
- [ ] Day 66-75: 약점 재학습
- [ ] Day 76-85: 모의고사 10회
- [ ] Day 86-88: 최종 정리

## 최근 기록 (최대 5개)
- [Day 60](daily/2026-07-10.md) — **[Phase 3 기출회독] 2024년 정처기 필기 (회독 3/4)** 5과목 균등 20문제 (Day 58·59 총 40문제와 완전 비중복, **2024년 격자 60칸 진입**). **1과목**(**럼바우 OMT 3모델 = 객체(정적 구조·클래스 다이어그램)·동적(시간 흐름·상태 다이어그램)·기능(데이터 흐름·DFD)** — "관계(Relational) 모델링"이라는 네 번째 모델은 존재하지 않음 완전 오답 함정 / **GoF 생성 5종 = Factory Method·Abstract Factory·Builder·Prototype·Singleton**(FMBPS) — **Decorator는 구조(Structural) 패턴**이며 생성 아님(뒤바꾸기 함정)·구조 7종 = ABCDFFP / **응집도 7단계(약→강) = 우연·논리·시간·절차·통신·순차·기능** — 응집도는 **강할수록 좋음**(우연이 최악, 기능이 최고)·"우연적이 이상적" 정반대 함정 / **결합도 7단계(약→강) = 자료·스탬프·제어·외부·공통·내용** — 결합도는 **약할수록 좋음**(자료가 최고, 내용이 최악)·"내용 결합도가 이상적" 정반대 함정) — **2과목**(**BST 삽입 후 중위 순회 = 항상 오름차순** [50,30,70,20,40,60,80] 삽입 → 중위(LNR) = **20 30 40 50 60 70 80** — 전위(NLR)와 뒤바꾸기 함정 / **선택 정렬 1회전** [9,3,5,1,7] → 최솟값 1과 첫 요소 9 교환 → **[1,3,5,9,7]** — 최종 결과 [1,3,5,7,9]를 1회전으로 오답 유도 / **블랙박스 5종 = 동등분할·경곗값·원인결과·오류추정·비교** — **기초 경로(V(G)=E−N+2)는 화이트박스** 대표 기법(뒤바꾸기 함정) / **통합 테스트 4종 = Top-down(스텁)·Bottom-up(드라이버)·Sandwich(병행)·Big Bang(한꺼번에)** — 빅뱅은 결함 추적 매우 어렵고 대규모 위험(소규모만 제한적)·"추적 쉬움·대규모 권장" 정반대 함정) — **3과목**(**함수 종속 3종 = 완전 FD(복합키 전체 종속)·부분 FD(복합키 일부만 종속·2NF 위반)·이행 FD(X→Y→Z 간접·3NF 위반)** — 부분 FD는 **복합키가 있을 때만 발생**, 단일 속성 기본키에서는 자동 완전 FD(뒤바꾸기 함정) / **2PL(2단계 로킹) = Growing(락 획득만)+Shrinking(락 해제만)·직렬 가능성 보장** — S/X-Lock 호환성 = **S-S만 ○, S-X·X-S·X-X 모두 ×**(모두 허용 정반대 함정) / **데이터 웨어하우스(Inmon 4특성) = 주제 지향·통합·시간 변형·비휘발성** vs OLAP 연산 5종(슬라이싱·다이싱·드릴다운·롤업·피벗) — **DW는 분석·의사결정 지원(DSS) 목적**·"OLTP 실시간 트랜잭션 처리 목적" 정반대 함정 / **NoSQL 4유형 = Key-Value(Redis·DynamoDB)·Document(MongoDB·CouchDB)·Column Family(Cassandra·HBase)·Graph(Neo4j)** + **CAP 정리 = 3가지 중 최대 2가지 동시 만족**(C·A·P 모두 만족 불가·P 필수 → CP vs AP)) — **4과목**(C 포인터: `p=arr`이면 `*(p+2)=arr[2]=30`, `*(arr+3)=40`, `p[4]=50` → **`30 40 50`**·포인터 산술은 요소 단위 이동 / **Java 오버라이딩 = 메서드는 동적 바인딩(인스턴스 타입) · 필드는 정적 바인딩(선언 타입)** · Dog d.hello()에서 자기 name="Dog" 출력 후 super.hello()로 부모 실행 → 부모의 name="Animal" 출력 → **`Hi, Dog / Hi, Animal`**·super는 부모 필드 접근 / Python 리스트 컴프리헨션 `[x*x for x in nums if x%2==0]`: 짝수만 필터(2,4,6) → 제곱 → **`[4, 16, 36]`**·필터 없이 전체 제곱(오답) / **OSI vs TCP/IP 대응 = OSI 상위 3개(응용·표현·세션) → TCP/IP 응용 1개**·OSI 물리+데이터링크 → TCP/IP 네트워크 접근·"OSI 상위 3개 → 인터넷 계층" 완전 오답 함정) — **5과목**(**CMMi 5단계 = 초기·관리·정의·정량적 관리·최적화** vs **SPICE 6단계(레벨 0 포함) = 불완전·수행·관리·확립·예측·최적화** — CMMi 5(Optimizing) = **정량 데이터 기반 지속 개선**("직관·경험 의존" 초기 1단계와 뒤바꾸기 함정) / **LOC = (a + 4m + b)/6** · **COCOMO 3유형 = 조직형(5만 라인 이하)·반분리형(30만 라인 이하)·내장형(30만 라인 초과)** — "애자일형"은 COCOMO 표준 유형이 아님(끼워넣기 함정)·FP 5요소 = EI·EO·EQ·ILF·EIF / **IPSec = L3(네트워크)** — AH(인증)·ESP(암호화+인증)·IKE(키교환)·Transport/Tunnel 모드·VPN 표준 vs **SSL/TLS = L4~L7(전송~응용 사이)** — TLS 1.3(RFC 8446)·"IPSec L7·SSL L3" 완전 뒤바꾸기 함정 / **사회공학 3종 = 피싱(이메일/사이트 유인)·스미싱(SMS+피싱)·파밍(hosts 파일 or DNS 변조·정상 URL 입력에도 위조 사이트로 유도)** — "파밍은 정확한 URL이면 무조건 정상 접속" 정반대 함정). Phase 3 15일차, D-28, **2024년 회독 3/4 · 격자 60칸 진입**. 내일 Day 61이 2024년 회독 4/4(최종회독, 격자 80칸 완결).
- [Day 59](daily/2026-07-09.md) — **[Phase 3 기출회독] 2024년 정처기 필기 (회독 2/4)** 5과목 균등 20문제 (Day 58 20문제와 완전 비중복, **2024년 격자 40칸 달성**). **1과목**(**애자일 대표 방법론 = Scrum(2~4주 Sprint·Product Backlog·Daily Scrum·Review·Retrospective)·XP(Pair·TDD·Refactoring·CI·Small Release)·Kanban(작업 시각화·WIP 제한·Flow 중심)** — "Scrum이 6개월 장기 계획·변경 금지" 폭포수와 완전 뒤바꾸기 함정 / **요구공학(RE) 5단계 = 도출(Elicitation)→분석(Analysis)→명세(Specification)→검증(Verification/Validation)→관리(Management)** — V모델/나선형 단계와 혼동 함정 / **UML 14종 = 구조 7(클래스·객체·컴포넌트·배치·복합구조·패키지·프로파일) + 행위 7(유스케이스·시퀀스·통신·상태·활동·타이밍·상호작용개요)** — **클래스는 구조(정적)**이며 행위 다이어그램 아님(뒤바꾸기 함정) / **아키텍처 스타일 = 계층형·클라이언트-서버·MVC·파이프-필터·이벤트 기반** — MVC는 **관심사 분리(Loose Coupling)**·"하나의 컴포넌트에 통합·Tight Coupling" 정반대 함정) — **2과목**(**큐 3대 응용 = BFS·프린터 대기열·라운드 로빈(RR) 스케줄링** — 후위 표기식은 스택 LIFO(뒤바꾸기 함정) / **병합 정렬 = 분할 정복·O(n log n)·안정(Stable)** BUT **보조 공간 O(n) 필요(제자리 아님)** — "병합이 제자리 O(1)" 힙 정렬(제자리·불안정)과 뒤바꾸기 함정 / **화이트박스 커버리지 4종 = 문장·분기·조건·경로** — 동치 분할·경곗값·원인-결과·오류 예측은 **블랙박스**(완전 뒤바꾸기 함정) / **ISO 9126 품질 6종 = 기능성·신뢰성·사용성·효율성·유지보수성·이식성** — "광고성(Advertisability)" 존재하지 않는 함정) — **3과목**(**E-R 카디널리티**: 1:1(주민번호)·1:N(부서-사원)·**M:N(학생-과목) → 별도 연결 테이블(Bridge) 필수 분해** — "M:N을 두 테이블만으로 표현" 완전 오답 / **관계 대수 순수 4종 = σ(선택, 튜플)·π(투영, 속성)·⋈(조인)·÷(나누기)** — **σ·π만 단항(Unary)**, **⋈·÷·∪·∩·−·× 전부 이항(Binary)** — 단항/이항 뒤바꾸기 함정 / **SQL 서브쿼리 3종 = 스칼라(SELECT 절, 단일값)·인라인 뷰(FROM 절, 가상 테이블)·중첩(WHERE 절, IN/EXISTS/ANY/ALL)** — "오직 SELECT 절만" 완전 오답 / **회복 기법 4종 = 즉시 갱신(UNDO+REDO)·지연 갱신(REDO만)·검사점(Checkpoint 이후만 처리)·그림자 페이징(2개 페이지 테이블·로그 없음·실패 시 그림자로 롤백)** — "그림자 페이징 복원 처리 불필요" 완전 오답) — **4과목**(C 구조체 포인터: `pp = &p`일 때 `pp->x = 10`과 `(*pp).y = 20`은 완전 동일 문법 → 원본 p 변경 → 출력 **`10 20`** / **Java finally = 예외 발생 여부 무관하게 항상 실행**(정상 종료·예외 종료 모두)·자원 해제 표준 패턴 — "예외 발생 시에만" 완전 오답 / Python 클로저: `make_counter()` 내부 `counter()`가 외부 `count` 캡처·`nonlocal count`로 쓰기 → 3회 호출 → **`1 2 3`** / **UDP = 8바이트 헤더·비연결·비신뢰·빠름**(RTP·DNS·SNMP·DHCP·VoIP) vs **TCP = 20~60바이트·연결·신뢰·느림** — "UDP가 무거운 헤더·3-way 필수" 완전 뒤바꾸기 함정) — **5과목**(**SW 방법론 시대순 = 구조적(1970s)→정보공학(1980s)→객체지향(1990s)→CBD(2000s)→애자일/DevOps** — **CBD가 구조적 이전** 완전 뒤바꾸기 함정·CBD 표준 = CORBA·EJB·.NET Component / **클라우드 배포 모델 4종 = Public(AWS·다수 고객·Pay-as-you-go)·Private(단일 조직 전용)·Hybrid(Public+Private)·Community(공통 관심사 다수 조직 공유)** — "Community가 단 하나의 개인 전용" 완전 오답 / **대칭키 = DES·3DES·AES·SEED·ARIA·IDEA(동일 키)** vs **비대칭키 = RSA·ECC·ElGamal·DSA(공개키·개인키 쌍)** — "RSA 대칭키·AES 비대칭키" 완전 뒤바꾸기 함정·실무 = 대칭키+비대칭키 하이브리드(TLS) / **DDoS 공격 = SYN Flood(백로그 큐 고갈)·UDP Flood(볼륨)·Smurf(ICMP 브로드캐스트 증폭)·Slowloris(저속 HTTP 헤더·연결 슬롯 고갈·L7)** — "Slowloris가 초당 수 GB 볼륨 공격" 완전 뒤바꾸기 함정). Phase 3 14일차, D-29, **2024년 회독 2/4 · 격자 40칸 달성**. 내일 Day 60부터 2024년 회독 3/4(신규 20문제 완전 비중복).
- [Day 58](daily/2026-07-08.md) — **[Phase 3 기출회독] 2024년 정처기 필기 (회독 1/4 진입)** 5과목 균등 20문제 (Day 54-57 2023년 4회독 80문제 완결에서 익힌 개념을 2024년 최신 출제 경향으로 확장). **1과목**(**SDLC 4모델**: 폭포수(순차·BDUF)·프로토타입(시제품·피드백)·나선형(위험 분석)·**애자일(변화 수용·짧은 반복)** — "애자일이 사전 문서 완벽 확정" 폭포수와 완전 뒤바꾸기 함정 / **MDA(Model Driven Architecture) = OMG 표준** — **PIM(플랫폼 독립) → PSM(플랫폼 종속) → Code** 자동 변환·UML/MOF/XMI/CWM 핵심 / **소프트웨어 3R = Reverse Engineering(역공학, 실행→문서 추출)·Re-engineering(재공학, 인터페이스 유지+내부 재개발)·Reuse(재사용)** — "역공학은 신규 개발만" 완전 오답 / **UI 4대 원칙 = 직관성·유효성·학습성·유연성** — 유연성 = 사용자 요구 수용·오류 허용("개발자 중심 고정" 정반대 함정)) — **2과목**(**스택 3대 응용 = 재귀(Return Address)·후위 표기식(Postfix)·괄호 검사(Matching Parentheses)** — 프린터/RR/BFS는 큐 FIFO(뒤바꾸기 함정) / **힙 정렬 = 완전 이진 트리·O(n log n)·제자리(in-place)** — "순차 탐색으로 최솟값 앞에" 선택 정렬 정반대 함정 / **IDE 기능 = 편집·구문 강조·자동 완성·컴파일·디버거·형상 관리 연동** — Hypervisor(가상화 계층)는 IDE 무관 함정 / **SCM 4대 활동 = 식별(Identification)·통제(Control, CCB 심의)·감사(Audit)·기록(Status Accounting, 이력 지속 기록)** — "최종 산출물만 관리" 완전 오답) — **3과목**(**Chen 표기법 = 마름모(관계)·사각형(개체)·타원(속성)** vs **Peterson(Crow's Foot, IE) = 까마귀 발·원·수직선·실선** — "Peterson이 마름모 필수" 완전 뒤바꾸기 함정·실무 CASE 도구(ERwin·PowerDesigner)는 Peterson 주류 / **3NF = 이행 함수 종속 제거** / **BCNF = 3NF + 모든 결정자가 후보키(강화)** — BCNF ⊆ 3NF·"BCNF가 3NF의 약한 형태" 정반대 함정 / **SQL 실행 순서 = FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY** — WHERE는 **행 필터**(집계 전), HAVING은 **그룹 필터**(집계 후) / **RBO(규칙 기반, 통계 무관·정적)** vs **CBO(비용 기반, 통계 활용·동적, 현대 RDBMS 기본)** — "RBO 실시간 통계 반영" CBO와 완전 뒤바꾸기 함정) — **4과목**(C 문자열: `strcat(a,b)` → "HelloWorld"·strlen=10(null 미포함)·`strcmp("abc","abd")` → 'c'<'d' 이므로 **음수(-1)** → 출력 **`10 HelloWorld -1`** / **Java 인터페이스 = 다중 상속 지원**(클래스는 단일 상속만)·`implements A, B, C`·필드는 public static final·Java 8+ default/static 메서드 — "인터페이스 단일 상속" 완전 오답 / Python 데코레이터 `@deco`는 `square = deco(square)`와 동일 → wrapper(3) = square(3) * 2 = 9 * 2 = **18** / **TCP = 연결 지향·신뢰성** vs UDP = 비연결·비신뢰 / **3-way = SYN→SYN+ACK→ACK**, **4-way 종료 = FIN→ACK→FIN→ACK**) — **5과목**(**Spring = 오픈소스(Apache 2.0)·IoC·DI·AOP 경량 프레임워크** — "상용 유료·소스 비공개" 완전 오답 / **전자정부 표준 프레임워크(eGovFrame) = Spring 기반 한국 정부 표준**·Struts = Apache Java 웹 MVC / **HDFS = 블록 128MB·기본 3복제·NameNode+DataNode** — "단일 노드 저장·장애 시 손실" 완전 오답 / **MapReduce = Map→Shuffle→Reduce**·YARN = 자원 관리(Hadoop 2.x+) / **DAC = 소유자 재량(chmod)** vs **MAC = 시스템 강제(보안 등급)** vs **RBAC = 역할 기반** — "DAC이 시스템 강제" MAC과 완전 뒤바꾸기 함정 / **APT = 장기·은밀·지속·표적 = 정찰→침투→지속·권한상승→내부이동→목적달성** — "1~2시간 단기 랜섬웨어" 정반대 함정·스턱스넷/SolarWinds 사례). Phase 3 13일차, D-30, **2024년 회독 1/4 진입**. 내일 Day 59부터 2024년 회독 2/4(신규 20문제 완전 비중복).
- [Day 57](daily/2026-07-07.md) — **[Phase 3 기출회독] 2023년 정처기 필기 (회독 4/4 · 최종회독)** 5과목 균등 20문제 (Day 54·55·56 총 60문제와 완전 비중복, **2023년 격자 80칸 완성**). **1과목**(**객체지향 4대 원칙 = 캡슐화·상속·다형성·추상화** — 추상화는 **일반화(Generalization)** 방향(특수화만 수행 정반대 함정), 상속은 특수화 / **GoF 23종 = 생성 5(FMBPS: Factory Method·Abstract Factory·Builder·Prototype·Singleton) + 구조 7(ABCDFFP: Adapter·Bridge·Composite·Decorator·Facade·Flyweight·Proxy) + 행위 11** — Adapter는 구조(생성 함정)·Iterator는 행위 / **ADL(Architecture Description Language) = Component + Connector + Configuration 3요소로 아키텍처를 형식적으로 기술** — Rapide·Wright·ACME·C2 / **인터페이스 요구사항 명세 = 이름·연계방식·식별자·데이터형식·프로토콜·오류처리·보안** — UI 스타일 가이드 무관) — **2과목**(**AVL 회전 4종 = LL(단순 우회전)·RR(단순 좌회전)·LR(좌→우 이중)·RL(우→좌 이중)** — "RL은 회전 안 한다" 완전 오답 / **퀵정렬 파티션 1회** `[5,3,8,4,9,1,7,2,6]` 피벗 5 → 좌측 4개(≤5) [3,4,1,2] · 피벗 [5] · 우측 4개(>5) [9,7,8,6] → **`[3,4,1,2,5,9,7,8,6]`** / **블랙박스 5종 = 동등분할·경곗값·원인결과·오류추정·비교** — **기초 경로(V(G) 기반)는 화이트박스**(블랙박스 함정) / DevOps 도구 Docker(컨테이너)·K8s(오케스트레이션)·Jenkins(CI/CD) — **Git = 버전관리**(하이퍼바이저 정반대 함정)) — **3과목**(**Chen ERD = 사각형(개체)·마름모(관계)·타원(속성)·이중 사각형(약한 개체)·이중 타원(다중값 속성)** — "개체=원" 완전 오답 / **SQL 4대 분류 DDL(CREATE·ALTER·DROP·TRUNCATE·RENAME) · DML(SELECT·INSERT·UPDATE·DELETE) · DCL(GRANT·REVOKE) · TCL(COMMIT·ROLLBACK·SAVEPOINT)** — TRUNCATE는 DDL(DML 함정)·GRANT는 DCL / **ACID = Atomicity(All-or-Nothing)·Consistency(정합성)·Isolation(중간결과 격리)·Durability(커밋 후 영구·로그로 자동 복구)** — "실패 시 자동 복구 안 됨"은 원자성/지속성 뒤섞기 함정 / **분산 DB 4투명성 = 위치(Location, 사이트 은닉)·중복(Replication, 복제 은닉)·병행(Concurrency, 동시성 은닉)·장애(Failure, 고장 은닉)**) — **4과목**(C 비트 연산 a=12(`1100`), b=10(`1010`) → **a&b=1000=8, a|b=1110=14, a^b=0110=6, a<<1=11000=24** → 출력 **`8 14 6 24`** / **Java super = 필드는 정적 바인딩(super.name=부모)·메서드는 명시적 부모 호출(super.sound())** — Dog d.sound() = "멍멍"(오버라이딩)·d.showParent() → super.name="Animal" · super.sound()="소리" → **`멍멍 / Animal / 소리`** / Python **`*args=튜플·**kwargs=딕셔너리`** — func(1,2,3,a=10,b=20) → sum(args)=6, len(kwargs)=2 → **`6 2`** / IPv6 = 128비트(IPv4=32비트의 약 2⁹⁶배 확장)·헤더 40바이트 고정·체크섬 제거·확장 헤더 분리 — "IPv6 주소 공간 축소" 완전 오답) — **5과목**(**CMMi 5단계 = 초기(Initial)·관리(Managed)·정의(Defined)·정량적 관리(Quantitatively Managed)·최적화(Optimizing)** — 레벨 5는 **정량 데이터 기반 지속 개선**("직관으로 개선" 완전 오답) / **CIA = 기밀성(Confidentiality, 암호화·접근제어)·무결성(Integrity, 해시·전자서명)·가용성(Availability, 이중화·백업)** / **SQLi 대응 = Prepared Statement + 입력검증(화이트리스트) + 최소권한 원칙 + ORM** — X-Frame-Options는 **클릭재킹 방어**(SQLi 무관 함정) / **클라우드 3모델 = IaaS(EC2·인프라)·PaaS(App Engine·플랫폼)·SaaS(Gmail·앱)** + **책임 공유 모델 = 계층별 분담**(IaaS↑ 사용자 책임 넓음, SaaS↓ 좁음) — "모두 사용자 단독 책임" 완전 오답). Phase 3 12일차, D-31, **2023년 회독 완결**. 내일 Day 58부터 2024년 회독 1/4 진입.
- [Day 56](daily/2026-07-06.md) — **[Phase 3 기출회독] 2023년 정처기 필기 (회독 3/4)** 5과목 균등 20문제 (Day 54·55 총 40문제와 완전 비중복, 2023년 격자 60칸 완성). **1과목**(**프레임워크 vs 라이브러리 = IoC(제어의 역전)** — 프레임워크가 개발자를 호출(할리우드 원칙)·라이브러리는 개발자가 호출·오픈소스 여부는 판별 기준 X / **GRASP 9패턴** — Information Expert(정보 잘 아는 자에게 책임)·Creator·Controller·Low Coupling/High Cohesion·Polymorphism·Pure Fabrication·Indirection·Protected Variations — Creator vs Info Expert 뒤바꾸기 함정 / **CBD 블랙박스 재사용** — 인터페이스만 공개·소스 공개 강제 X(오답 함정)·EJB/COM/DCOM·표준 인터페이스로 결합도↓ / **재사용 4대 = 합성·생성·재구성(구조만 개선)·재공학(인터페이스 유지+내부 재개발)** — 재구성 vs 재공학 뒤바꾸기 함정) — **2과목**(DFS 사전순 트레이싱 예시 그래프(A-B,A-C,B-D,B-E,C-F,E-F) → **A→B→D→E→F→C**(BFS는 A→B→C→D→E→F) / **해시 충돌 = 체이닝(연결리스트)** vs **개방주소법(선형·이차·이중해싱 = 빈 슬롯 탐색)** — Java HashMap은 Chaining+RBTree(JDK8+) / **ESB = 표준 버스·느슨 결합·SOA/MSA** vs EAI(Hub&Spoke 중앙집중·단일 장애점) vs Point-to-Point(1:1 배선 폭발) vs ETL(DW 데이터 이관) / **회귀(Regression) = 기존 통과 케이스 재수행·부작용 방지**·스모크(빌드 검증 최소)·새니티(좁은 얕은 재검증)·알파/베타(인수)) — **3과목**(**뷰(View) = 가상 테이블·자체 인덱스 생성 불가**(Materialized View만 예외)·ALTER VIEW 불가·CREATE OR REPLACE 사용·WITH CHECK OPTION / **Sequence CURRVAL = 현재 세션 마지막 값**·NEXTVAL(다음 값 발급)과 뒤바꾸기 함정·ROWNUM/ROWID 별개 / **역정규화 5유형 = 테이블 통합(조인 비용↓)·분할(수직/수평)·중복 속성·파생 속성(계산결과)·이력 테이블** — 통합 vs 분할 정반대 함정 / **트리거 = 이벤트 자동 실행(INSERT/UPDATE/DELETE)·NEW/OLD 참조·반환값 X·매개변수 X** vs 프로시저(EXECUTE)·함수(RETURN)) — **4과목**(**realloc 확장부는 초기화 X(쓰레기값)** — calloc(3, sizeof(int))+p[3]=40 후 5칸 재할당 → **10 20 0 40 쓰레기**(malloc-초기화X vs calloc-0초기화 vs realloc-확장부초기화X) / **Java HashMap = 해시테이블·순서 X·평균 O(1)** vs TreeMap(RBTree·정렬·O(log n))·ArrayList(인덱스 O(1)·검색 O(n))·LinkedList(노드참조 O(1)) — JDK8+ Chaining+RBTree 승격 / **세마포어 vs 뮤텍스 = 소유(Ownership) 유무·N자원 vs 1자원**·"기능적 동일" 함정 X·이진 세마포어 ≠ 뮤텍스·P(wait)/V(signal) / **BGP = AS간·경로벡터·EGP·인터넷 백본·TCP 179** vs RIP(홉15·DV·IGP)·OSPF(다익스트라·LS·IGP)·PIM(멀티캐스트·RPF)) — **5과목**(대칭키 4종 **DES(56bit·페이스텔·취약)·AES(128~256bit·SPN·현행)·SEED(128·KISA)·ARIA(128~256·국정원·SPN)** — ARIA를 RSA 기반 공개키로 오해 함정 / 비대칭키 수학 근거 = **RSA 소인수분해·ECC/DSA/ElGamal/DH 이산대수·ECC는 짧은 키 강도** vs AES는 대칭키·연산 복잡도 근거(소인수분해 아님) / **SBOM = 소프트웨어 자재 명세서(오픈소스·서드파티 컴포넌트·버전·라이선스)** — SPDX/CycloneDX/SWID·Log4Shell 이후 의무화·EO 14028·CVE 신속 매칭·공급망 보안 / **3-2-1 백업 = 사본 3·매체 2·오프사이트 1** — 랜섬웨어 대응 오프사이트·오프라인·불변(Immutable)·에어갭·확장 3-2-1-1-0·RTO/RPO/BCP/DRP). Phase 3 11일차, D-32, 2023 회독 3/4. 내일 Day 57 = 2023년 회독 4/4(최종회독).
---
자동 생성: 매일 새벽 4시 KST.
